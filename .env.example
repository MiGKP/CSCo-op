# For local development (SQLite):
DATABASE_URL="file:./dev.db"

# For production deployment on Vercel + Neon (PostgreSQL):
# Change provider in prisma/schema.prisma to "postgresql" and set:
# DATABASE_URL="postgresql://user:password@ep-sample-pooler.neon.tech/neondb?sslmode=require"

# Bearer Token for Student Portal API access
STUDENT_WEB_API_TOKEN="coop_secret_token_2026_student_portal"
